# GIT clone ff rebase, merge FF only

When your changes are different than on the remote and u get the question if u want to merge, rebase or xx.FF only simply do

```shell
git branch -D branchname
```
<small>not origin</small>