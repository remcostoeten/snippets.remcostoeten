# GIT upstream branch

Prevent GIT wanting to set upstream branch every time you try to push a new feature.

```shell
git config --global push.default current
```

<small>This has to be done for every project.</small>
