# Log build details, handy for when comparing optimizations

```shell filename="Log Build Details" copy
(time (rm -rf node_modules .next && pnpm i && format && pnpm run build)) 2>&1 | tee build-info.txt
```
