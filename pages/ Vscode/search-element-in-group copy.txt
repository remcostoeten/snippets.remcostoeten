**/group/**/*.jsx

# Title

Some text which describes the `problem` and `solution`.

```fileextensionname,so,could,be,shell,or,bash,or,javascript filename="this shows on top of the code box"
code goes here
```

#```/* so a example would look like so: */
#bash filename="example.sh"
#grep -r -E '(?!.*\\static\\sass\\).*\.jsx$' /path/to/folder/or/workspace
#```

#tutorial goes here